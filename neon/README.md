# Neon

Manage Neon serverless Postgres — projects, branches, and SQL over Neon's hosted remote MCP.

Connects to Neon's official remote MCP at `https://mcp.neon.tech/mcp`. Auth is handled in the browser via OAuth on first use — no API key to paste.

## Install

Use Fleet's Marketplace and click **Get** on the Neon card.
